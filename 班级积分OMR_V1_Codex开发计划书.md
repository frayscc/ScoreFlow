# 班级积分 OMR 系统 V1：Codex 开发计划书

版本：1.0｜日期：2026-09-13｜项目暂名：ScoreFlow（若仓库已有名称，沿用原名）

## 0. 给 Codex 的执行指令

你需要开发一个完全本地运行的班级积分工具。本文件既是开发计划，也是业务规格和验收依据。请直接检查当前仓库、制定任务清单并分阶段实现，不要重新发起已经完成的需求问卷，不要只返回架构建议。

先阅读仓库已有 AGENTS.md、README、代码和相关规格；保留有效实现，不为了套技术栈推倒重写。若旧文档与本文件冲突，以本文件为当前产品要求，并列出实际冲突。普通实现细节自行决策，记录到 docs/decisions.md；只有影响核心纸表规则、数据完整性或确实缺少外部条件时才询问。

开发范围是本地软件，不发布网站，不建立公网服务，不接入云数据库。先打通真实主流程，再完善界面。禁止用固定演示结果冒充 OMR、真实计分或成功打包。

按阶段推进并更新 docs/progress.md：完成内容、验证证据、未完成项、下一步。遇到真实打印样本或另一操作系统不可用时，明确标记待验收，并继续能够独立完成的工作，不伪造测试结果。

## 1. 产品目标与边界

教师在电脑上建立积分周期、生成纸表。全班一名固定记录员在纸上登记；周期结束公开原表并处理申诉，再扫描、识别、复核、入账、关闭周期，最后导出 PDF 给教室展示。

### 1.1 已确定的使用场景

- 当前班级49人，7组，每组7人；不得沿用旧的37人名单。
- 一张 A4 双面纸覆盖全班49名学生；正面加分、背面扣分。
- 不是每组一张，也不是每周固定生成一张。
- 一个积分周期先生成纸表01，槽位不够时生成02、03等补充表。
- 全班一名固定记录员；组长不获得登记权限，不做学生账号。
- 周期通常两周，后期可一个月；创建时设置开始日期及预计结束日期。
- 周期内不做实时电子排名，不要求逐事件录入日期。
- 申诉在扫描前处理，教师复核后才入账；正常流程不因普通申诉反复重扫。
- 教师端仅本机使用；给学生展示靠导出的周期报告PDF。
- 核心操作完全离线，无需注册、API密钥、Python安装、Node安装或Docker。

### 1.2 V1 不包含

公网部署、多设备实时同步、学生端、家长端、角色权限体系、手机直连上传、扫码在线查询、扫描仪驱动集成、OCR手写备注转文字、AI判分、游戏化商城、云端备份服务、自动更新服务。

允许代码支持多个本地班级项目，但无需开发学校级管理平台。名单人数不得在数据库内写死；49人/7组是首版纸表验收基准，超出版式容量应明确提示，不能静默拆成多张或缩到无法填写。

## 2. 产品形态与技术方案

采用“浏览器界面＋本地启动器＋本地Python引擎”，Windows和macOS共用业务代码、分别打包。

| 层次 | 默认方案 | 要求 |
|---|---|---|
| 前端 | React＋TypeScript＋Vite | 生产静态资源全部随包携带 |
| 本地服务 | Python＋FastAPI＋Uvicorn | 同源提供页面与API，只监听127.0.0.1 |
| 图像处理 | OpenCV＋NumPy | CPU工作，不依赖GPU或大模型 |
| PDF生成 | ReportLab或等效可精确控制尺寸的库 | 嵌入可再分发中文字体，单位使用毫米 |
| PDF栅格化 | 优先评估pypdfium2 | 核对打包兼容性和许可证，不能要求用户安装外部命令 |
| 数据 | SQLite＋版本化迁移 | 后端是唯一持久化入口 |
| 打包 | PyInstaller onedir或经验证的等效方案 | 不强求单EXE；所有运行依赖随包 |
| 测试 | pytest；前端选Vitest/Playwright | 重点验证识别、计分、重复入账、恢复与主流程 |

具体依赖版本由实施时核验兼容后锁定。不要直接使用漂移的 latest；记录运行时版本和第三方许可证。上述选型是实现默认，不是要求同时引入所有可选库。

### 2.1 启动和退出

1. 用户解压对应平台版本，双击启动器。
2. 启动器检查数据目录权限、项目锁和已有实例。
3. 启动本机服务，等待健康检查通过，再打开默认浏览器。
4. 页面显示真实的加载状态、可读错误和重试入口。
5. 软件内提供明确的“退出软件”，先结束或安全停止任务、保存数据，再终止服务。

不要声称关闭浏览器标签就必然退出后台。普通关闭标签保留本次本地服务，重复启动重新打开已有实例；用软件内退出完成关闭。需要后台管理时用轻量启动器窗口，不引入完整桌面壳作为首版前提。

端口动态分配；随机会话凭据、Host/Origin校验及写请求保护应防止其他网页滥调本地API。凭据不写入常规日志，不使用全开放CORS。文件路径访问限定在项目或明确选择的导入导出范围。

### 2.2 分发与数据目录

- Windows x64：解压后运行启动EXE。
- macOS Apple Silicon：优先交付可双击应用启动器，界面仍在浏览器；Intel Mac后续按条件扩展，不宣称已支持。
- 原生依赖在对应系统和架构构建、冒烟验证。不能在Mac上把Windows构建脚本写完就声称Windows包已交付。[PyInstaller官方说明](https://pyinstaller.org/en/stable/operating-mode.html)
- 正常使用不要求管理员权限。签名、公证状况和实际测试过的系统版本必须写入发布说明，不能指导用户关闭系统整体安全保护。
- 程序资源与用户数据分离。首次选择本地可写项目目录；升级替换程序不会覆盖项目。
- 不将工作数据库放在浏览器IndexedDB中；不把运行中的数据库当作网盘同步对象。
- 一个项目同时只允许一个写实例。跨电脑使用通过项目包导出/恢复，不做双向合并。

## 3. 业务规则：必须按此实现

### 3.1 学生、小组与周期快照

- 每名学生有不可变内部ID及长期固定的班级学号；学号不因换组改变。
- 纸面只显示“学号＋姓名”，没有组内序号。默认1～9不补零。
- 学号唯一，姓名允许重名；历史学生学号不重复分配给新人。
- 第1～7组是稳定组号；组员、组长均可在下一周期调整。
- 当前周期开始后，学生名单、学号、姓名、组别、组长、行顺序、规则与分值全部保存快照并冻结。
- 组长必须来自本周期本组成员；组长身份只用于显示及历史记录，不影响积分计算。
- 下一周期可沿用上一周期名单，再修改分组/组长；不得连带修改过去周期。
- 周期中途不新增学生、不换组；转入学生下一周期加入。
- 当前49人正式名单未随本文件提供：使用明确标为演示的49人名单开发，提供正式名单导入入口，不能把历史不完整名单拼成真实名单。

### 3.2 积分规则

每人每个新周期从100分开始，不结转上一周期分数。每个有效槽位代表一次事件，默认一次加5分或扣5分。

| 正面加分项目 | 默认分值 | 背面扣分项目 | 默认分值 |
|---|---:|---|---:|
| 作业＋ | +5 | 作业－ | -5 |
| 课堂＋ | +5 | 课堂－ | -5 |
| 成绩＋ | +5 | 值日－ | -5 |
| 服务＋ | +5 | 迟到－ | -5 |
| 值日＋ | +5 | 晚归－ | -5 |
| 活动＋ | +5 | 其他－ | -5 |
| 其他＋ | +5 | — | — |

不另加成绩－、服务－、活动－、纪律－、卫生项目；不把基础分当成OMR项目。支持在下一周期开始前改名、排序、停用、调整分值；增加自定义项目必须经过模板容量校验，不能挤掉既有槽位。加分项目分值为正、扣分项目为负，默认以5分为步长。

V1实施默认：个人不设最高/最低分，不自动截断到0；小组以成员本周期最终分之和排名，同时展示人数和平均分。当前等人数条件下总分和平均分排序一致。若未来不等人数，需清楚标注口径，不默默改变排名规则。并列分数采用并列名次，不杜撰额外加赛条件。

个人最终分＝100＋本周期全部有效已入账记录的分值之和。

### 3.3 槽位与申诉

每名学生每个项目固定5个槽位。计数与填写位置无关。

| 槽位状态 | 含义 | 计分 |
|---|---|---|
| 空白 | 未登记 | 0 |
| 单斜线 `/` 或 `\` | 有效事件 | 1次 |
| X | 原事件作废 | 0 |
| 无法判断 | 待教师确认 | 未确认前不入账 |

`/ 空 / 空 X`必须直接计2次，不报非连续异常。`X`不当成可重新填写的空白格。不要把所有非空痕迹都当有效事件；勾、圆、涂黑、擦改等无法可靠归类时进入复核。

教师处理纸面申诉后，由固定记录员将错误斜线补为X，遗漏事件补在任意空白格；无空格则使用补充表。不擦除、不覆盖、不用修正液。系统不检查事件发生时间、填写顺序或“是否从前往后”。

“其他”原因写在独立备注区，用班级学号定位，例如“15：主动搬器材”。备注不进入槽位ROI，不执行OCR。复核时提供备注图像和可选简短录入；不因没输入电子备注就强制教师逐条审核。电子备注可关联纸表、学生和项目，不能假装识别出具体事件日期。

### 3.4 周期流程与状态

草稿 → 进行中 → 待结算 → 已关闭。

- 草稿：编辑名单、组长、规则、时间；尚未发布正式纸表。
- 开始周期：原子保存快照，进入进行中，生成纸表01。
- 进行中：以纸面登记，必要时生成补充表；电子界面明确“本周期尚未结算”。
- 预计结束日期到达只提醒，不自动关闭、不自动创建下一周期；本地软件未运行时，下次打开再提醒。
- 待结算：教师完成公开与申诉后，主动进入扫描流程。
- 已关闭：所有有效纸表完成处理，教师手动关闭，生成正式报告。

入账可以按纸表逐张完成，但所有相关界面在周期关闭前标为“结算中/未最终确认”。首页可展示上一已完成周期，必须标明周期名称，不能伪装为当前实时分数。

关闭条件：每张有效签发纸表均已入账，或整表由教师明确标为“未使用作废”；没有缺面、待复核或失败任务。已填写纸表不能为跳过问题而自动作废。

## 4. 纸表 PDF：首要工程验证

### 4.1 硬要求

- A4纵向、双面打印，一张物理纸正反面均包含全班49人。
- 正面7个加分项目，背面6个扣分项目，每项5格。
- 从上到下按小组排列学生；同组“小组”列合并7行，不新增小组标题行。
- 列结构：小组｜学号｜姓名｜各项目槽位。
- 正反面使用完全相同的学生行位置与行顺序。
- 每面有班级、周期、表号、正/背面、唯一身份二维码、定位标记和紧凑填写说明。
- 保留独立小型备注区，不能与定位标记或识别槽位重叠。
- 黑白打印可用，不依赖颜色区分；不设置“本行需复核”勾选框。
- “纸表PDF”供打印，“周期报告PDF”供展示，界面和文件名均区分。

### 4.2 尺寸预算与禁止事项

此前“方框5毫米、49人、正面35格可以轻松放下”的估算不成立。35个5毫米宽的方框本身就占175毫米，还未计算学生信息列、槽间距和项目分隔。

先制作精确参数化模板。可从以下候选预算试排，但这些尺寸不是已经验证的最终结果：

- 页面210×297毫米，四周各约7毫米安全边距，可用196×283毫米。
- 标题/身份区约16毫米，表头约9毫米，49行×4.8毫米＝235.2毫米，备注约15毫米，总计275.2毫米。
- 学生信息列合计约40毫米，项目区剩156毫米，正面每项约22.3毫米。
- 每项5个约3.5毫米见方的槽，槽间距约0.5毫米，占19.5毫米；余量安排项目间隔和边线。
- 中文姓名尝试8～9磅，长姓名单独验证，不静默截断。

准确预算必须包含二维码静区、四角标记、线宽、字体实际高度及打印不可打印边缘。3.5毫米槽是否易写、扫描后是否可靠，必须用真实纸样检验。不得仅凭PDF屏幕效果宣布可行，也不能自行改为A3、每组一张、减少5格或删除项目。

如果实测无法满足，输出候选版式、尺寸及失败证据，请用户选择核心约束调整；其他独立开发继续。

### 4.3 模板与纸表身份

模板生成器同时产出PDF和机器可读manifest，二者共享同一坐标来源。manifest至少保存：模板版本、纸张尺寸、定位点、二维码区域、行到周期学生映射、规则列、每槽ROI、备注ROI、布局哈希。

二维码至少包括格式版本、项目ID、周期ID、纸表ID、面别、模板标识/哈希。二维码不需要携带全班姓名；识别根据本地快照映射，不靠OCR猜姓名或当前名单顺序。

正式纸表01、02各有独立ID，正反面共享纸表ID但面别不同。再次导出同一张纸表保持同一身份；“生成补充表”才创建新身份。提醒不要把同号复印件当新表独立登记，系统无法自动把同号两张不同手写记录安全合并。

纸表01已有记录不抄到02。任何项目槽位不足都可以生成补充表，补充表仍覆盖全班与全部项目。周期内所有有效纸表累计。

## 5. 导入、OMR与教师复核

### 5.1 输入与处理流水线

支持扫描仪多页PDF、JPG、PNG及手机扫描App生成的PDF。手机负责采集，经AirDrop、数据线或文件传输到电脑再导入；界面推荐原图或文件方式传输，避免压缩丢失笔迹。

流水线：保存原文件与哈希 → PDF逐页栅格化/图片方向处理 → 识别页面身份 → 校验所属项目及周期 → 定位与透视校正 → 图像质量检查 → 按模板提取ROI → 槽位分类 → 聚合预览 → 复核 → 入账。

- 扫描仪为首版主要验收，建议300dpi；手机拍照标为实验功能。
- 页序任意，允许只导正面后补背面；不能根据PDF页码奇偶判断面别。
- 支持旋转页；严重裁切、定位缺失、未知模板或未知项目明确提示，不自动套当前模板。
- 二维码无法识别时，可输入短编号后核对预览与定位，不允许静默猜测。
- 保留原图和校正图，不能覆盖原证据。正背面分别校正，不假设双面打印完全对齐。
- 处理黑白线框、边框残留、细斜线、蓝/黑笔、X交叉和背面透印；避免把打印边框当笔迹。
- 多页任务采用有界队列，CPU默认单工作进程起步，内存不随整份PDF无限增长。

首版采用可解释的传统图像处理：配准、去模板/边框影响、连通域或线段方向特征等。具体算法通过样本比较选定，不要求先训练模型。

### 5.2 复核原则

低置信度不自动判为空白或有效。前端优先呈现有疑点的槽位及学生、项目、原图上下文；支持放大、键盘快捷确认、一键确认其余明确结果。教师修改的是槽位判定，并保留自动结果和人工结果。

只在确实影响计分时提示：笔迹难辨、图像不合格、身份不匹配、重复面内容冲突等。跳格、无事件日期、没有电子备注、某学生次数较多均不应成为强制异常。

每页显示空白/有效/X/待复核数量和预测加扣分；每表显示学生计数汇总。人工确认后显示入账影响，并提供明确确认动作，不能扫描后立即静默加分。

### 5.3 正反面、重复与重扫

- 同一纸表两面均已确认才可整表入账。
- 某面确实空白可由教师点击“确认该面空白”；存下确认时间与来源，无扫描图也可完成该面。
- 原始文件哈希用于快速重复提示；业务防重键是纸表ID＋面别及入账记录，不能只靠文件哈希。
- 同一面拍两次、换文件名或重新生成PDF，仍是同一业务页面。
- 未入账的重扫生成新候选版本，教师选择采用哪一版；不把两个版本计数相加。
- 已入账页面再导入只提示已入账，不追加计分。
- “确认空白”后又导入有记录的页面属于冲突，教师明确撤回空白确认并重新复核。
- 每个槽位/汇总来源必须可追溯到唯一已采用的识别版本。

## 6. 数据模型与一致性

可按工程实际合并表，但下列概念与约束不能缺失。

| 实体 | 核心内容 |
|---|---|
| Project/Class | 项目ID、班级名称、学年、数据版本 |
| Student | 不变ID、固定学号、姓名、在班状态 |
| Group | 项目内稳定组号 |
| Period | 开始/预计结束、状态、基础分、关闭时间、结果版本 |
| PeriodStudent | 学生ID、当期学号/姓名/组号/排序快照 |
| PeriodGroup | 当期组长与组别信息快照 |
| Rule / PeriodRule | 可编辑规则与冻结的周期规则快照 |
| TemplateVersion | 几何参数、manifest、字体/版式版本、哈希 |
| PaperSheet / PaperSide | 表号、模板、两面状态、未使用作废状态 |
| ScanAsset / RecognitionRun | 原件、页索引、校正图、算法版本、候选版本 |
| SlotObservation | 槽ID、自动分类、置信度、人工分类、采用版本 |
| PostingBatch / LedgerEntry | 入账批次、来源、学生、规则、次数、单位分值、金额 |
| Audit / Export | 人工修改、撤销原因、报告版本与数据来源 |

关键唯一约束：班级学号、周期学生、周期表号、纸表面别、每个来源的有效入账关系。外键开启，核心金额用整数，不用浮点数。

计分引擎独立于前端与OMR；前端不能自己提交任意总分覆盖后端。可按“纸表＋学生＋项目”聚合流水，但必须保留有效槽ID列表。记录日期只能叫入账时间，不伪造事件发生日期。

### 6.1 原子性与撤销

- 整表确认、流水写入、纸表状态更新在同一事务完成，失败全部回滚。
- 重复点击/重试使用幂等键与数据库约束，不产生重复积分。
- 扫描文件采用先写临时文件、校验、原子落盘再关联数据库；失败时可清理孤立暂存文件。
- 最小恢复能力：误入账可显式撤销整表入账，生成关联反向流水并记录原因，再采用新版本重新入账。不能直接删除历史记录掩盖变更。
- 关闭周期后只读；发现明确录入错误，教师可通过维护入口显式重新打开、留原因、修正、重新关闭，报告版本递增。此为纠错能力，不增加学生申诉系统。
- 后续所有汇总从有效账本计算或可由账本重建，不依赖不可验证的缓存总数。

## 7. 页面与交互

只做完成任务需要的页面，不先搭几十个空页面。中文界面，普通教师不用理解ROI、数据库事务等技术词。

1. **项目与名单**：新建/打开项目，粘贴表格或导入UTF-8 CSV名单；字段学号、姓名、组号、是否组长。预览后一次性导入，重复学号报错，导入失败不留半份名单。
2. **周期设置**：复制上一周期名单、调整分组/组长和规则、设置日期、开始冻结。
3. **纸表管理**：生成01/补充表，下载/打开PDF，查看两面处理状态、未使用表作废。
4. **扫描复核**：拖入多文件、真实任务进度、页身份、疑点复核、计数汇总、整表入账。
5. **周期结果**：个人分数、小组排名、项目统计、明细证据入口、手动关闭、报告导出。
6. **备份与设置**：项目包导出/恢复、目录、版本、日志、退出软件。

长任务必须显示阶段与已完成页数；无法测量的阶段用不定进度，不伪造匀速百分比。可取消尚未提交的任务，退出时处理进行中任务。重开后区分已完成、待继续和失败，不自动重复入账。

## 8. 周期报告 PDF

生成前要求周期已关闭；允许关闭前预览，但必须带“未结算草稿”标识。正式PDF包含班级、周期日期、生成时间、数据版本与统计口径。

首版两个预设：

- 教室展示版：小组排名、各组人数和均分、维度统计、正向表现；不默认突出个人倒数和敏感扣分原因。
- 教师存档版：全班个人总分、组别、各项目有效次数与分值、加扣分汇总、纸表来源和已录入备注。

教师可以选择导出全班个人明细；用户已接受全班查看，不增加登录权限。报告用中文图表和可读表格，长内容分页，不把49人完整明细挤在单张展示页。可采用展示版16:9横版、存档版A4的实现默认，设置中标明。

没有逐事件日期，不制作每日趋势、实时动态或周报。比较个人进步时只比较相同学生的已关闭周期，标明原始分差；周期长度或规则不同应说明不可直接等同比较。由于组员可变，不能默认把同组号跨周期视为同一批学生计算进步。

报告从已保存数据生成，文字和字体完整嵌入，离线打开正常。相同数据重复导出统计值一致；重新结算后生成新版本，不覆盖先前报告的历史来源。

## 9. 项目包导出与恢复

提供未加密的 `.scorepack`，本质是ZIP；不需要密码。至少包含：manifest.json、SQLite一致性快照、模板及manifest、纸表PDF、原始扫描、校正证据、配置与必要报告。

manifest记录项目ID、格式/数据库版本、生成时间、文件路径、大小和SHA-256。使用SQLite Backup API等一致性方法，不直接复制正在写入的.db文件当完整备份。[SQLite官方备份说明](https://www.sqlite.org/backup.html)

为保证数据库与证据同一时点，导出时暂停项目写入并固定文件清单，完成快照后安全打包；证据文件采用不可变版本。先写临时包，完成校验后原子更名。

恢复先解压到暂存目录，检查路径穿越、压缩炸弹限制、格式版本、数据库完整性、引用文件与校验值。未知新版本或损坏包拒绝恢复，不修改当前项目。默认“恢复为独立本地项目目录”，保留包内业务身份，不自动合并两个项目。若用户选择替换当前项目，先导出当前完整备份再切换。

导入导出跨Windows/macOS使用相对路径及UTF-8。进程锁、绝对机器路径、运行凭据不进入可移植业务数据。首版必须手动完整导出/恢复；定时全量备份不是已确认需求，不先建设复杂调度系统。可做入账/恢复前轻量安全快照，但不能把仅数据库快照称为完整项目备份。

## 10. 分阶段计划与交付门槛

### Phase 0：仓库评估与最小骨架

交付：现状评估、任务清单、决策记录、前后端本地启动、健康检查、项目目录约定、依赖锁定及打包冒烟脚本。

验收：本机浏览器能访问本地界面；仅回环监听；不加载远程字体或CDN。尽早用最小包验证原生依赖，不能把打包可行性拖到最后。

### Phase 1：49人纸表与识别技术样机（优先级最高）

交付：49人演示名单、正背面两页样表、版式manifest、精确尺寸说明、模拟标记生成器、最小定位及槽位分类程序、打印回收测试说明。

必须先验证：正面1715格（49×7×5）、背面1470格（49×6×5），合计3185格；无缺行、重叠、越界；空白、两种斜线、X、跳格及边缘笔迹可测试。

让用户按100%实际大小双面打印，用常见0.5毫米中性笔填写样本，扫描回传。无真实样本时，本阶段只能标“数字验证完成、实扫待验证”，同时继续数据/UI开发。禁止发布“已通过纸面OMR验收”。

### Phase 2：名单、周期、快照及纸表签发

交付：SQLite模型与迁移、名单导入、7组分组/组长、周期状态机、规则快照、带唯一ID的正式纸表及补充表。

验收：换组换组长不影响旧周期；重复学号拒绝；周期开始后禁止改冻结字段；补充表保留相同快照但身份独立；正式名单与演示项目隔离。

### Phase 3：批量导入、识别与复核

交付：PDF/图像导入、有界任务队列、页面身份匹配、质量检查、四态识别、疑点优先复核、空白面确认、单面重扫版本管理。

验收：乱序、旋转、重复文件、重复业务面、错项目、缺面均正确处理；跳格不报错；笔迹不确定可人工确认；未完成复核不允许入账。

### Phase 4：可靠入账、关闭与报告

交付：独立计分引擎、幂等事务、账本与撤销、周期关闭、教师/展示报告。

验收：两张纸表累计准确，X不计分，多次点击只记一次，失败回滚，未使用表显式作废后可关闭；报告与账本逐项相符，真实事件日期不被虚构。

### Phase 5：备份恢复与完整便携包

交付：scorepack导出/恢复、版本迁移、安全退出、升级保留数据、Windows x64及macOS arm64分平台构建脚本和实际可用包（按可用构建环境分别标状态）。

验收：无Python/Node的干净环境可启动；断网完成整条流程；中文/空格路径可用；恢复包后纸表仍可匹配且已入账表不会重复计分；坏包不覆盖当前数据。

### Phase 6：真实试运行与V1交付

交付：完整源码、可复现构建说明、已验证的平台包、用户快速指南、测试报告、已知限制、第三方许可证、示例项目和样表。

以真实打印扫描样本跑通一次完整周期。记录硬件、样本规模、识别耗时、复核数量、错判及失败页。只交付已经实际完成的部分；未拿到Windows构建机/真实扫描件时明确列出相应待验收项。

## 11. 必须覆盖的验收用例

| 编号 | 场景 | 预期 |
|---|---|---|
| A01 | 49人正式模板 | 两面各49行，正面1715槽/背面1470槽，无溢出 |
| A02 | `/ 空 / 空 X` | 有效2次，不触发跳格异常 |
| A03 | `/ X / 空 空` | 有效2次，X不计分 |
| A04 | 同学生表01有2次课堂＋，表02有1次 | 加15分，不重复抄算 |
| A05 | 学生有3次＋5和1次－5 | 最终110分 |
| A06 | 全班全空白 | 每人100分，每组700分，全班4900分 |
| A07 | PDF改名重复导入，或同面重新拍照 | 不重复入账；内容不同提示版本冲突 |
| A08 | 先正面、后背面 | 缺面时不能入账；两面确认后可入账 |
| A09 | 背面确认空白 | 有审计记录，允许整表入账 |
| A10 | 空白确认后导入非空背面 | 明确冲突，不自动追加 |
| A11 | 重复点击入账、任务重试 | 仅一份有效积分 |
| A12 | 写入事务中途异常 | 不出现半张表入账 |
| A13 | 下一周期换组长和组员 | 历史PDF、分组及归属不变 |
| A14 | 学生重名、学号不同 | 通过内部ID/学号准确区分 |
| A15 | 模糊笔迹、裁切、未知二维码 | 不静默记0或套错学生，进入明确处理流程 |
| A16 | 未使用补充表尚未处理 | 提醒处理，显式作废后可关闭 |
| A17 | 撤销误入账，再重扫入账 | 保留历史且最终总分正确 |
| A18 | 完整包跨平台恢复 | 中文正常、证据齐全、业务ID及防重有效 |
| A19 | 备份损坏/不支持版本 | 原项目不受影响 |
| A20 | 关闭后重开软件 | 成绩、纸表、复核结果与报告均保留 |
| A21 | 未安装开发环境且断网运行 | 生成、导入、识别、计分、报告、备份全可用 |

### 11.1 OMR质量验证口径

不以大量空白格带来的“总体准确率很高”掩盖有效笔迹错判。分别报告空白误报率、有效斜线召回、X识别、自动接受后的错误率、转人工比例及学生/项目计数准确率。

建议首轮真实验收集至少10个扫描面，包含至少300个有效斜线与100个X，分布到不同学生、项目、页边及两种笔色；另留未参与调参的验证样本。模拟图仅用于单元/回归测试，不能代替这些真实证据。

发布门槛：固定实扫验收集经必要人工复核后的计数与人工真值完全一致；不允许已知静默错计、身份串行或重复计分缺陷。清晰标准扫描的自动识别覆盖率目标≥95%，这是工程目标而非现成保证；若未达到需披露原因与复核负担。不能通过“全部转人工”宣称识别功能完成。

测量PDF栅格化、校正、分类分别耗时及峰值内存，不预先保证“5分钟完成”。手机拍照单独报告效果，不混入扫描仪成绩掩盖差异。

## 12. 实施时的重要约束汇总

1. 本地单教师产品，没有学生端和公网服务器。
2. 一张A4双面全班49人，不按组分纸；固定一名记录员。
3. 每周期一张起步，写满出独立补充表；不是每周固定一张。
4. 学号＋姓名，没有组内序号；小组列合并，同组连续排列。
5. 有效斜线按次数计，X作废，跳格不触发异常。
6. 先纸面申诉，再扫描复核入账；不建立频繁重扫的常规流程。
7. 学生身份固定，7组组号固定，组长组员可在新周期调整，历史快照不变。
8. 正反面确认齐全才入账；真实空白面允许教师确认。
9. 原生依赖随平台包携带，普通教师无须安装环境。
10. 优先证明纸面可写、扫描可认、积分可靠，再优化视觉效果。

请从 Phase 0 开始，随后优先执行 Phase 1。每个阶段报告可检查的文件、运行方式和验证结果，持续推进可完成的后续阶段，不在每阶段结束机械询问“是否继续”。
