# Phase 2 验证报告

日期：2026-09-13

## 交付范围

- SQLite schema v3 与 v1→v2→v3 迁移路径。
- 自定义 1—20 组，组员人数可不同；当前 A4 模板总人数上限 49。
- UTF-8/BOM CSV 名单预检与原子导入，学号唯一，姓名可重复。
- 学生姓名、组别、组长调整；当前资料修改不影响已经开始的周期快照。
- 正反面规则名称、顺序、停用（从当前列表移除）和 5 分步长分值设置。
- 周期草稿创建与修改；开始后冻结名单、组长、行序和规则。
- 正式纸表01、补充表及稳定业务身份；同一纸表重复下载不重新签发。
- 模板版本、布局哈希和字体哈希持久化。
- 纸表状态列表与“未使用作废”保护。

## 自动验证

- `pytest`：27 项通过。
- 覆盖：重复学号整批回滚、错误组长不产生半份快照、历史快照不随当前资料变化、规则多次换版、草稿冻结、八组不等人数、49 人正式生成、补充表独立 ID、重复下载内容一致、未使用作废。
- `npm run build`：通过。
- `npm audit --audit-level=low`：0 个已知漏洞。
- PDF：正面 1715 槽、背面 1470 槽；Noto Sans SC Regular 400 字体子集已嵌入。

## 尚不属于本阶段完成证明

- 真实打印扫描的 OMR 效果仍待纸样；模拟识别不能替代实扫验收。
- 扫描导入、复核和版本冲突处理属于 Phase 3。
- Windows/macOS 最终分发包属于 Phase 5，当前只有依赖与打包预检。
