# 班级积分 OMR V2：Codex 修改与开源发布计划书

日期：2026-09-13｜基于现有V1增量修改｜暂称ScoreFlow，已有项目名则沿用

## 0. 直接执行指令与本轮授权

请在现有项目上完成以下修改、验证、GitHub开源上传和Windows便携版自动构建。不要重写整个系统，不要重新询问已确定业务规则，不要只交付计划或工作流占位文件。

用户已明确要求将该项目上传到自己的GitHub并开源，以及使用GitHub托管的Windows Runner构建Windows便携版。这些动作属于本轮授权，不需要再次询问是否上传/是否开源/是否运行Actions。先完成可审查的代码、文档及必要验证，再执行发布。不绕过真实权限限制或分支保护。

先读AGENTS.md、V1规格、代码、数据库迁移、打包配置及现有Git远程；核实功能而非相信“V1已经全部完成”。本文件覆盖V1中与本轮冲突的交互设计，其余业务规则保持。

交付标准：修改真实生效；GitHub公共仓库可访问；远程Windows Actions确实成功；可下载便携ZIP；Windows和Mac应用均接入项目图标。没有账户认证或目标仓库确实无法确定时，仅针对具体阻塞询问，其他本地工作继续完成。

V2是本轮需求名称，不强制软件版本跳到2.0.0。按仓库版本约定递增；尚未真实扫描验收的版本可以发布为预发布版，但必须给出可下载构建。

## 1. 不变的核心规则

- 完全本地、单教师使用：浏览器界面＋本地引擎；不增加云服务器、学生账号或手机直连。
- 当前49人、7组、每组7人；学号长期固定；纸面学号＋姓名，没有组内序号。
- 一张A4双面包含全班，正面7项加分，背面6项扣分，每人每项5格。
- 每周期先生成一张，写满生成独立编号补充表；全班一名记录员。
- 斜线有效、X作废、空白不计；跳格不报异常，按有效次数统计。
- 每周期100分起，默认每次±5分；周期快照和历史积分不可被新分组污染。
- 申诉在纸面扫描前完成；正反面均确认后整表入账，空白面允许教师明确确认。
- 备份包、重复扫描防重、事务入账、证据追溯继续有效。
- 本轮不以用户提供Notability测试样本为前提；用户准备在V2完成后再测试。

## 2. 修改一：粘贴名单，页面分组与选组长

### 2.1 主入口改为粘贴名单

“导入名单”的首选入口是多行文本框：只需学号与姓名，不要求CSV，不要求提前填写组号或组长字段。现有CSV导入保留为次要入口。

支持下列格式混用，一行一人：

```text
1 张三
02 李四
25号 王五
26，赵六
27,钱七
28、孙八
29<TAB>周九
```

最后一行表示从Excel复制出的真实制表符，不是要求用户输入字符串<TAB>。

解析要求：

1. 忽略空行、清理首尾空白，兼容全角数字及常见分隔符。
2. 只解析行首学号；学号后的“号”可选；内部将01与1视为同一学号。
3. 姓名保留内部合理字符、少数民族姓名中点等，不随意截断或替换。
4. Excel两列粘贴直接支持；明确的“学号 姓名”表头可跳过并说明，模糊行不能静默丢弃。
5. 实时显示解析人数与预览，错误定位到原始行。重复学号、缺姓名、非法学号阻止提交，重名仅提示且允许。
6. 当前班级提示“已识别49人”；不把49写死为通用导入限制，后续打印仍按模板容量检查。
7. 预览确认后原子保存；报错不留下半份名单；重复点击不重复新增。
8. 已有名单再次导入时，按学号展示新增/不变/姓名差异预览；默认保留已有人员和分组，不以一份不完整粘贴自动删除其余学生，不静默覆盖姓名。

### 2.2 分组工作台

名单导入后进入分组页：左侧待分组学生池，右侧第1～7组卡片。卡片显示组号、人数和成员学号姓名；49人应便于浏览和查找，不要求横向排七列。

- 拖拽学生到组内完成分组；支持跨组移动、返回待分组区、组内排序。
- 多选若干学生后通过“移入第X组”批量操作；提供点击菜单作为拖拽替代，键盘也能完成主要操作。
- 搜索学号或姓名，搜索不改变保存顺序。
- 每组顶部有“拖到这里设为组长”区域，成员另有“设为组长”按钮。
- 组长仍是本组一名成员，不能显示成第8个人或重复计算人数。
- 新组长接替时原组长成为普通成员。组长移出本组时清空该组组长，提示重新指定，不静默选别人。
- 将其他组学生拖到组长区域时，清楚完成“移入本组＋指定组长”，保持全班唯一归属，并及时更新原组人数及原组长状态。
- 显示“待分组N人、已分组M人”；缺组长、超员、缺员标记在对应组上。
- 支持撤销最近一次移动/组长调整；拖拽失败或保存失败回到一致状态。

### 2.3 保存与冻结

草稿允许不完整分组保存，重启后恢复进度；当前49人班级开始周期前必须49人全部唯一归组、每组7人、各有1名组长。

只允许在周期草稿中改变本周期分组。已开始周期查看只读分组，提供“为下一周期调整”入口，将名单复制到新的草稿；不能借此改动已经签发的纸表快照。主名单更新不得重写历史PeriodStudent。

## 3. 修改二：扫描导入记录管理

新增清楚可见的“导入记录”入口；按导入批次展示，展开可见各PDF页面。记录：文件名、时间、页数、周期、纸表号、面别、识别版本、处理状态、有效次数/积分影响；支持预览、状态筛选和批量操作。

### 3.1 三种对象必须分清

- 导入批次/文件：用户这次上传的文件及其页面。
- 纸表身份：生成的表01/表02及正反面；删除扫描不删除已签发纸表。
- 入账：已经发生的积分及对应证据；不能直接删文件让账本失去来源。

### 3.2 按状态提供操作

| 状态 | 应提供操作 | 处理原则 |
|---|---|---|
| 等待/识别中 | 取消任务、取消后删除 | 停止工作进程写入后再处理 |
| 导入失败/识别失败 | 重试、删除 | 不影响纸表身份及积分 |
| 已识别未确认 | 预览、复核、重新识别、替换、删除 | 删除后可重新导入同号纸表 |
| 已确认未入账 | 取消确认、替换、删除 | 清除对应面确认；不能误保留“已确认” |
| 已入账 | 查看证据、撤销入账并移除 | 原子撤销整张表的积分，保留必要审计证据 |
| 已撤销 | 查看历史、从活动列表移除 | 不把历史证据当成垃圾彻底清除 |

普通未入账测试记录应可直接删除。多个记录批量删除前明确列出可删除/需撤销数量；不默默跳过失败项、不为删除测试记录强制删除整个周期。

### 3.3 删除的完整性要求

- 删除当前采用的未入账扫描面，该面恢复待扫描；不能自动选择另一个旧版本，也不能自动确认空白。
- 删除未采用的旧候选版本，不影响当前采用版本或另一面的状态。
- 删除导入批次中的一页，其他页正常保留；共享原始PDF有引用时不得物理删除。无任何引用的临时文件才可清理。
- 正背面共同入账，所以撤销一面关联的入账时按整表撤销，提示受影响人数和分值；另一面证据保留，重入账必须重新满足完整性条件。
- 即使空白表积分变化为0，只要已入账，也需要撤销入账身份后才能替换。
- 已关闭周期的记录走已有重新打开/纠错流程，不绕过冻结状态直接删除。
- 正在识别时取消/删除应防止“后台任务结束后又把删除记录写回来”。
- 对未入账记录的删除不保留会阻止再次导入的永久防重标记；对已入账记录的防重不能因此失效。

用户当前复现路径是“生成空白PDF→原样导入→想删除”。此路径必须单独回归，删除后同一PDF能再导入且仍识别为空白。

## 4. 同步补齐的四项体验

### 4.1 真实识别预览

展示引擎实际使用的栅格图/校正图，不仅是浏览器独立PDF预览。叠加有效、X、待复核分类，允许开关叠加层，颜色外配形状或文字说明。点击槽位可放大核对和修改，保留自动/人工判定。

### 4.2 空白面处理

识别结果整面为空时提示“识别为全空白，请核对预览”，提供确认空白及移除本次导入。不能因检测到0个标记就自动确认入账；PDF笔迹漏渲染也可能产生假空白。

### 4.3 同一纸表的新版本

同号同面再次导入，区分文件完全重复和内容变化。未入账时允许查看新旧预览并明确采用新版本，重新复核；已入账时先走撤销流程。任何情况下都不能把同一面的两个版本相加。

### 4.4 独立演示项目

使用49名虚构学生提供演示项目，可独立重置。正式项目不出现“一键清空所有记录”的随意入口。演示项目ID与正式项目隔离；重置后旧演示纸表不能误匹配新项目。

## 5. PDF电子笔迹兼容：本轮修技术，用户之后验收

用户将在V2完成后用iPad Notability填写测试，不要求用户现在提供样本，也不把这个等待当作本轮发布阻塞。

检查PDF渲染器是否显示注释层及笔迹外观。程序化构造含可见笔迹批注的测试PDF，同时测试普通页面内容中的斜线/X，两种都必须进入实际OMR输入图。不能只测试原始模板或只在前端看得到批注。

交付文档说明：原PDF导入Notability→黑色普通笔画斜线/X→导出PDF→传回电脑→导入软件。页面尺寸、二维码、定位点保持完整。

自动测试覆盖空白、1斜线、2个跳格斜线、1个X、2斜线加1个X、5斜线，以及同表更新版本。Notability真实导出和纸质扫描分别标“待用户验收”，不可宣称实际测试过Notability。电子PDF通过不代表打印扫描通过。

## 6. 项目图标：设计和真正接入

为该项目提供统一品牌图标，Windows/Mac使用同一图形。设计方向：极简积分登记表与斜线记录符号，表达“纸表记录、统计与成长”；高辨识度，少量深蓝/青绿色，轮廓清楚，无文字、无密集49人格子、无真实学生信息。

如用户提供本轮生成的图标设计稿，优先以它为源素材制作平台图标。如工作区尚无该附件，不要让开发阻塞：根据上述方向制作原创可维护的矢量图标，并如实说明来源；禁止用默认框架图标充数，也不要依赖未授权的付费图像API。

交付并提交到源码仓库：

- 图标母版PNG，1024×1024，正确透明背景；若原生矢量制作则同时保存真实SVG源文件，不能把位图塞进SVG冒充矢量。
- Windows多尺寸ICO，至少16、24、32、48、64、128、256像素。
- macOS ICNS及生成用iconset，覆盖常用1x/2x规格；1024像素用于512@2x。
- favicon及网页内品牌图；生成脚本与源素材授权/来源说明。

接入范围：Windows启动EXE资源、原生启动器窗口（如有）；Mac .app的资源与Info.plist；本地网页favicon及关于页。普通浏览器窗口的任务栏图标可能仍属于浏览器，不要求伪造浏览器应用身份。

不是只把app.ico/app.icns放进目录：检查打包spec/config真实引用它们，检查构建产物包含正确资源，在实际系统验证Finder/资源管理器图标。Mac如果只有.command文件，应提供带图标、可双击的轻量.app启动器，业务界面仍用浏览器，不为图标重写成Electron/Tauri。

小尺寸另检查16/32/48像素可读性，避免细线缩小后消失。保留留白、透明边缘，无白底方块或锯齿。PyInstaller支持Windows ICO和Mac应用ICNS，具体参数以现有打包方式核对。[官方说明](https://pyinstaller.org/en/stable/usage.html)

## 7. GitHub公共开源发布

### 7.1 确定仓库并实际上传

1. 检查git status、远程origin、已有仓库名和当前登录GitHub账号；用gh auth status或现有连接能力验证，不输出token。
2. 已有明确属于用户的本项目仓库时沿用；无远程时，在当前已认证的用户个人账号下创建同名公共仓库，名称冲突时用与项目相关的后缀，记录最终名称。
3. 远程指向他人仓库或多个账号无法确定时，只澄清目标身份；不得擅自往上游发布或修改他人仓库可见性。
4. 用户已授权本项目开源。现有项目私有仓库确认归属后可按授权公开；但先检查仓库内容及将被公开的历史，不能把仅添加.gitignore当成清理历史。
5. 提交有意义的变更记录并推送。受保护分支走PR和规定检查，不强推、不绕过保护。

### 7.2 可公开内容及文档

公开源码、迁移、构建脚本、依赖锁文件、原创图标、虚构示例、测试和使用说明。排除真实学生名单、积分、扫描件、项目数据库、备份包、私人路径和凭据。检查Git历史及CI/Release打包清单，真实数据不随便携包分发。

如果历史已经包含不适合公开的数据，保持原私有仓库不动，在独立干净目录只导入可公开代码并建立公共仓库，不重写或删除用户原历史。发现凭据泄漏需明确告知具体类型及位置，不把凭据本身显示出来。

已有开源许可证则沿用；无许可证时默认采用MIT作为本项目原创代码的发布许可，并检查依赖/字体的分发义务。第三方代码及素材保留各自声明，不能把其许可改为MIT；存在不兼容条件就解决或具体报告。

README至少说明：软件用途、中文截图（虚构数据）、Windows/Mac下载、解压启动、名单粘贴分组、纸表打印、扫描与删除/撤销、备份、离线工作方式、手机/电子笔迹试验状态、开发构建命令、许可证。

新增CHANGELOG、第三方声明和发布说明。发布是源代码及桌面包开源，不部署公网版本，不启用GitHub Pages。

## 8. GitHub Actions：Windows便携版真实构建

### 8.1 工作流范围

至少提供：

- CI：PR/主分支运行相关测试；PR不发布Release，不具有写权限。
- Windows构建：主分支push及workflow_dispatch可触发，使用GitHub托管Windows x64 Runner；默认核验后固定windows-2022，若依赖确需更新镜像则记录原因。不要配置为家里电脑/self-hosted Runner。
- Release：版本标签触发，对该标签精确commit重新构建与验证，发布便携ZIP和校验值。普通push产物放Actions artifacts，不让每次push都生成正式Release。

实施时核验Actions当前可用版本并锁定可信版本/完整commit SHA。依赖安装使用锁文件；Node/Python选择与现有项目兼容的版本，不写未经验证的版本组合。

GitHub的runs-on选择托管镜像，镜像标签与架构需要明确检查；日志记录实际OS、Python架构和commit。[Runner官方说明](https://docs.github.com/en/actions/how-tos/write-workflows/choose-where-workflows-run/choose-the-runner-for-a-job)

### 8.2 Windows job必须完成的步骤

1. 检出指定commit，安装锁定开发依赖，生成前端生产资源。
2. 执行名单解析、分组快照、删除/撤销、重复导入及计分回归。
3. 验证ICO可读且包含多尺寸，运行现有PyInstaller onedir或等效构建。
4. 包含Python原生运行依赖、OpenCV/PDF引擎、字体、网页资源、图标、必要许可证；用户无需安装Python/Node。
5. 从dist复制完整包到新的含中文及空格路径，切换工作目录，清除开发PYTHONPATH/虚拟环境依赖，通过打包后的EXE运行冒烟测试。
6. 冒烟覆盖：启动并健康检查、生成49人纸表、渲染/识别空白与标记PDF、退出后无遗留任务。测试不能调用源码Python函数替代打包EXE。可增加--self-test或--no-browser参数供CI使用，不暴露在普通教师操作流程。
7. 测试后确保删除测试项目数据；明确allowlist打包，只压缩应用目录。
8. 生成 `<项目名>-<版本>-windows-x64-portable.zip`，及SHA256SUMS.txt；记录应用版本与源commit。
9. 上传Artifacts，文件缺失即失败，合理保留期限（例如14天），附构建/测试摘要。

不要通过“pip install成功”或“PyInstaller exit 0”宣布便携版通过。GitHub Runner自带大量软件，即使CI通过也要如实区分“打包产物冒烟通过”和“无开发环境真实Windows桌面人工验收”。不把Server镜像测试冒充Windows 10/11全部版本验证。

Artifacts用于构建记录与任务间传递，用户长期下载使用Release附件。[Artifacts官方说明](https://docs.github.com/en/actions/tutorials/store-and-share-data)

### 8.3 Release与权限

一般构建采用contents: read；仅Release job赋予contents: write，使用GITHUB_TOKEN，不要求用户为普通同仓库发布提供个人PAT。避免pull_request_target执行不可信代码，不为fork PR开放发布权限。[GITHUB_TOKEN官方说明](https://docs.github.com/en/actions/tutorials/authenticate-with-github_token)

Release job等待必要构建成功，使用同一版本/commit的资产；不能取“最近一次成功构建”冒充当前标签产物。发布说明注明变化、系统架构、启动方式、未签名/公证状态、实际验证范围及待用户验证项。未实扫验收时设置pre-release，而不是宣称正式稳定版。

没有付费签名证书不阻塞便携包分发，但不能声称已签名，不指导关闭整个SmartScreen/Gatekeeper。不购买服务或开启付费大型Runner。

必须主动触发首次Windows构建，等待实际结束，失败则查看日志修复并重跑。提供成功run URL、源commit和Windows下载链接；仅推送YAML不算完成。

### 8.4 Mac交付

Mac也必须接入图标并构建更新后的.app。优先使用现有可用Mac开发机产出arm64包；可增加经架构验证的GitHub macOS job。不能在Windows Runner上产出并声称验证过Mac原生包。只在实际测试的架构上标注支持，不把arm64包称为Universal。

Mac打包保留执行权限、应用目录结构和启动资源；通过归档后的包重新解压测试，不只测构建目录。能够构建时将Mac ZIP与同版本Windows包共同附于Release；确无Mac环境时交付图标源与配置，列出Mac构建待完成，Windows交付继续。

## 9. V2执行阶段

| 阶段 | 工作 | 完成证据 |
|---|---|---|
| V2-0 | 核对现有实现、保护项目数据、建立修改清单 | 简短现状报告、具体缺口、迁移方案 |
| V2-1 | 多格式名单解析、拖拽/批量分组、组长与草稿 | 49人演示录入和分组截图、相关测试 |
| V2-2 | 导入记录管理、删除、撤销、新版本与空白处理 | 状态回归、重导测试、计分一致性 |
| V2-3 | 识别预览、演示重置、PDF批注兼容 | 实际栅格图对照及合成笔迹测试 |
| V2-4 | 图标设计接入、本地包、开源文档和许可 | 图标源/ICO/ICNS、产物资源检查 |
| V2-5 | 推送公共GitHub、Actions首次构建与修复 | 公共仓库URL、成功Windows run及ZIP |
| V2-6 | 发布可下载版本、说明验证边界 | Release URL、校验值、最终完成/待验收清单 |

每阶段完成后更新progress，不机械询问是否继续。阶段顺序可为效率调整，但不能跳过数据一致性验证或先公开真实数据再补清理。

## 10. 必须覆盖的验收清单

| 编号 | 场景 | 预期 |
|---|---|---|
| V201 | 混合空格、逗号、号字、Tab的49人名单 | 正确解析，无需CSV |
| V202 | 01和1重复、缺姓名、重名 | 前两者定位报错；重名可导入 |
| V203 | 粘贴不完整名单到已有班级 | 不自动删除未出现学生或清空分组 |
| V204 | 拖拽与批量跨组移动 | 一人唯一归属，人数即时正确 |
| V205 | 换组长、组长移出、拖入组长位 | 无重复成员，原组长状态正确 |
| V206 | 未完成分组保存重启 | 草稿恢复；不能带缺项开始周期 |
| V207 | 修改下一周期分组 | 历史纸表和积分归属不变 |
| V208 | 空白原PDF导入→删除→重新导入 | 成功且仍为0次，无防重死锁 |
| V209 | 删除当前候选/非当前候选 | 只影响对应版本，面状态准确 |
| V210 | 多页PDF删除其中一页 | 其他页面及共享原件证据正常 |
| V211 | 已入账记录要求移除，包括0分表 | 必须撤销整表，账本和身份正确 |
| V212 | 识别中取消/删除 | 无迟到写回、无孤立积分 |
| V213 | 同表电子PDF新增笔迹后再导入 | 可替换未入账版本，不累计两份 |
| V214 | 页面内容笔迹和PDF批注笔迹 | 实际OMR输入图都可见并正确处理 |
| V215 | 2个跳格斜线＋1个X | 计2次，不报跳格异常 |
| V216 | 演示重置 | 正式项目不受影响 |
| V217 | V1已有项目升级及备份恢复 | 历史数据、证据与防重保持有效 |
| V218 | Windows打包EXE在新路径自测 | 无源码路径依赖，中文路径正常 |
| V219 | 应用图标 | ICO/ICNS正确且实际应用引用 |
| V220 | 公共仓库及Release内容 | 无真实班级数据、无凭据，许可证完整 |
| V221 | Windows GitHub Actions | 实际成功，有可下载本次commit构建 |
| V222 | 下载ZIP解压核对 | 校验值一致、启动文件与依赖齐全 |

回归重点是有实际风险的行为，不为简单文案和样式堆叠重复测试。UI截图可以证明布局，不能替代数据规则与打包产物测试。

## 11. Codex最终回复必须给出

1. 已完成修改的简要说明和必要截图。
2. GitHub公共仓库URL、提交commit、Windows Actions成功run URL。
3. Release页面及Windows便携ZIP下载链接；Mac包可用时同时给出。
4. 图标源、ICO、ICNS在仓库中的位置与实际接入情况。
5. 测试结果：自动测试、打包自测、真实人工测试分别说明。
6. 尚未验证的Notability真实导出、纸质扫描或Mac环境等具体项，不虚报完成。

现在开始检查仓库并实施，持续推进至上述可交付状态。
