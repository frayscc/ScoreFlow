# V2-1 验证报告：名单与周期分组

验证日期：2026-09-14

## 交付范围

- 友好文本名单解析、实时预览及原子增量导入。
- 周期级分组草稿、拖拽/批量移动、组长、搜索、撤销与修订冲突保护。
- SQLite v8→v9 增量迁移，周期开始前校验和开始后历史快照冻结。
- 旧版四列 CSV 仍作为高级入口保留。

## 自动化结果

- `pytest`：54 项通过。
- `npm --prefix frontend run build`：TypeScript 检查与 Vite 生产构建通过。
- `npm --prefix frontend audit --audit-level=high`：0 个已知高危漏洞。

覆盖的 V2-1 回归包括：

- 混用空格、逗号、顿号、Tab、全角数字和少数民族姓名中点。
- 明确表头、缺失姓名、非法行、`01/1` 重复、允许重名。
- 不完整二次导入不删人、不清空分组、不静默改名；重复点击幂等。
- 49 人分组草稿保存/重启、7组每组7人与同组组长校验。
- 旧修订号冲突拒绝、非法组长事务回滚、不完整分组禁止开始。
- 下一周期修改不影响已开始周期快照。
- HTTP API 完整路径：预览、导入、读取草稿、保存草稿、开始周期和开始后只读。
- Scorepack 对新分组草稿表的剪枝与恢复回归。

## 迁移验证

对实际 V1 数据库的临时副本执行 v8→v9 迁移，原文件未修改。升级后 schema 版本为 9，项目、周期、纸表和账本的记录数保持，`PRAGMA foreign_key_check` 为 0 项。

## 界面证据

- [49 人名单粘贴预览](images/v2-roster-paste.png)
- [49 人周期分组工作台](images/v2-grouping-workbench.png)

截图数据均为虚构内容，不含真实学生资料。

## 未在本阶段宣称完成的内容

扫描导入记录删除/撤销、电子 PDF 笔迹兼容、平台图标、GitHub Actions 与 Windows Release 属于后续 V2 阶段。
